node_modules
npm-debug.log
.npmrc
coverage/
.aws-config.json
awsconfig
/emails/dist
/public_gen
/tmp
/build
vendor/phantomjs/phantomjs

docs/AWS_S3_BUCKET
docs/GIT_BRANCH
docs/VERSION
docs/GITCOMMIT
docs/changed-files
docs/changed-files

# locally required config files
public/css/*.min.css

# Editor and OS junk
.DS_Store
.idea/
.vscode/
*.sublime-workspace
*.swp
*.iml

/data/*
/bin/*

conf/custom.ini
fig.yml
profile.cov
grafana
.notouch

# dist/ is entirely build output; `.empty` is the only tracked file in it.
# (A bare `tsc` run emits .js/.js.map next to it, so ignore the whole tree.)
/dist/*
!/dist/.empty

# Antora documentation
/build/
/public/
